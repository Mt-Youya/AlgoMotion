# @algomotion/algo-wasm

Rust implementations for performance-sensitive algorithms.

```bash
cargo test
pnpm --filter @algomotion/algo-wasm build:wasm
```

The second command requires `wasm-pack`. It generates the publishable JavaScript and WebAssembly bindings in `pkg/`.
